<%= bootstrap_form_for [:admins, @category] do |f| %>
  <%= f.text_field :name, class: "form-control" %>

  <%= f.submit @category.new_record? ? t("buttons.add") : t("buttons.update"), class: "btn btn-primary" %>
<% end %>
